# Carmine Visuals — Environment Configuration

# Server
PORT=3000
NODE_ENV=development

# Session & JWT
SESSION_SECRET=dev-session-secret-change-in-production
JWT_SECRET=dev-jwt-secret-change-in-production

# Database (leave empty to use SQLite; set to 'mssql' to use SQL Server)
DB_TYPE=sqlite
# DB_TYPE=mssql

# SQL Server (only used if DB_TYPE=mssql)
# SQLSERVER_CONN=Data Source=DESKTOP-IF07HU6\SQLEXPRESS;Integrated Security=True;Encrypt=True;TrustServerCertificate=True;

# SMTP (optional; leave empty to log activation links to console)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@carminevisuals.local
# SMTP_SECURE=false

# Admin seed credentials (used on first npm run init-db)
SEED_ADMIN_USER=admin
SEED_ADMIN_PASS=Admin@123
